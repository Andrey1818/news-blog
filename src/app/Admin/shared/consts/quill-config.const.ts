import {quillConfig} from "../interfeces/app.interfeces";

export const configQuill: quillConfig = {
  toolbar: [
    ['bold', 'italic', 'underline', 'strike', 'link',],
    ['code-block'],
    [{'header': [1, 2, 3, 4, 5, 6, false]}],
  ]
}
